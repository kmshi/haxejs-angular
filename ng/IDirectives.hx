package ng;

/**
 * directive should be public static variable field = function return something to guide $compiler to render UI
 * @author Richard Shi
 */

@:autoBuild(ng.macro.InjectionBuilder.build("directive"))
interface IDirectives
{
}